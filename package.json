{
  "name": "ship-agency-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run start --workspace=frontend",
    "server": "npm run start --workspace=backend",
    "build": "npm run build --workspace=frontend"
  },
  "dependencies": {
    "axios": "1.7.9",
    "react-scripts": "^3.0.1"
  }
}
